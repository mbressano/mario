let a = 15;
let div = 1
while (div <= a) {
  if (a % div == 0) {
    console.log("el divisor es " + div);
  }
  div++;
  //esto es una prueba de modificacion
  // esto es una segunda modificacion
  // tercera modifcacion
  //cuarta modificacion
  //quinta modifcacion

}